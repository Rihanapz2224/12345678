# لیست برای ذخیره اعداد زوج و فرد
L1 = [] 
 # لیست اعداد زوج
L2 = [] 
 # لیست اعداد فرد
# پیمایش از 1 تا 100
for number in range(1, 101):
    # بررسی اینکه آیا عدد زوج است
    if number % 2 == 0:
        L1.append(number) 
         # اگر زوج بود به ال 1 اضافه میکنیم 
    else:
        L2.append(number)  
        # اگر فرد بود به ال 2 اضافه میکنیم
# چاپ لیست اعداد زوج
print("List of even numbers (L1):", L1)
# چاپ لیست اعداد فرد
print("List of odd numbers (L2):", L2)
