# لیست ورودی که اعضای تکراری دارد
List1 = [1, 2, 2, 2, 3, 3, 4, 5, 5]
# لیست خالی برای اعضای غیر تکراری
List2 = []
# هر عدد را در لیست بررسی میکنیم
for n in List1:
    # آیا عدد در لیست غیر تکراری وجود دارد یا خیر
    if n not in List2:
        # اگر وجود ندارد آن را به لیست غیرتکراری اضافه میکنیم
        List2.append(n)
        # چاپ نتیجه
print ("List without duplicote membrs" , List2)
